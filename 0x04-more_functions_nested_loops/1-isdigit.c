#include "main.h"

/**
 * _isdigit - checks if 'c' is a digit or not
 * @c: character to check
 * Return: 1 if character is uppercase else return 0
 */

int _isdigit(int c)
{
	int result;

	result = (if (c >= 48 && c <= 57) ? 1 : 0;
	return (result);
}
