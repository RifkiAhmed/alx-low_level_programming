#include "main.h"

/**
 * _islower - checks for lowercase character
 * _putchar: prints alphabets
 * @c: character to check
 *
 * Return: 1 if character is lowercase or 0 if otherwise
 */

int _islower(int c)
{
	int c;

	(c <= 122 && c >= 98) ? return (1) :  return (0);
}
