#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer
 * @n: interger to be comupted
 * Return: always absolute value, end of program
 */

int _abs(int n)
{

	return (abs(n));
}
